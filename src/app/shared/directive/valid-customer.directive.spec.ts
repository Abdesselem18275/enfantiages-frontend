import { ValidCustomerDirective } from './valid-customer.directive';

describe('ValidCustomerDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidCustomerDirective();
    expect(directive).toBeTruthy();
  });
});
